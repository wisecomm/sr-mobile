<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springrest.domain.user.repository.UserInfoMapper">
    <resultMap id="UserInfoResultMap" type="com.example.springrest.domain.user.model.entity.UserInfo">
        <id property="userId" column="USER_ID"/>
        <result property="userEmail" column="USER_EMAIL"/>
        <result property="userMobile" column="USER_MOBILE"/>
        <result property="userName" column="USER_NAME"/>
        <result property="userNick" column="USER_NICK"/>
        <result property="userPwd" column="USER_PWD"/>
        <result property="userMsg" column="USER_MSG"/>
        <result property="userDesc" column="USER_DESC"/>
        <result property="userStatCd" column="USER_STAT_CD"/>
        <result property="userSnsid" column="USER_SNSID"/>
        <result property="useYn" column="USE_YN"/>
        <result property="sysInsertDtm" column="SYS_INSERT_DTM"/>
        <result property="sysInsertUserId" column="SYS_INSERT_USER_ID"/>
        <result property="sysUpdateDtm" column="SYS_UPDATE_DTM"/>
        <result property="sysUpdateUserId" column="SYS_UPDATE_USER_ID"/>
        <collection property="roles" ofType="com.example.springrest.domain.user.model.enums.UserRole">
            <result column="ROLE_ID"/>
        </collection>
    </resultMap>

    <sql id="UserInfoColumns">
        u.USER_ID, u.USER_EMAIL, u.USER_MOBILE, u.USER_NAME, u.USER_NICK, u.USER_PWD, u.USER_MSG, u.USER_DESC, 
        u.USER_STAT_CD, u.USER_SNSID, u.USE_YN, 
        u.SYS_INSERT_DTM, u.SYS_INSERT_USER_ID, u.SYS_UPDATE_DTM, u.SYS_UPDATE_USER_ID
    </sql>

    <select id="findById" resultMap="UserInfoResultMap">
        SELECT 
            <include refid="UserInfoColumns"/>,
            r.ROLE_ID
        FROM CHMM_USER_INFO u
        LEFT JOIN CHMM_USER_ROLE_MAP r ON u.USER_ID = r.USER_ID AND r.USE_YN = '1'
        WHERE u.USER_ID = #{userId}
    </select>

    <select id="findAll" resultMap="UserInfoResultMap">
        SELECT 
            <include refid="UserInfoColumns"/>,
            r.ROLE_ID
        FROM CHMM_USER_INFO u
        LEFT JOIN CHMM_USER_ROLE_MAP r ON u.USER_ID = r.USER_ID AND r.USE_YN = '1'
        <where>
            <if test="userName != null and userName != ''">
                AND u.USER_NAME LIKE CONCAT('%', #{userName}, '%')
            </if>
            <if test="startDate != null and startDate != ''">
                AND u.SYS_INSERT_DTM &gt;= #{startDate}::timestamp
            </if>
            <if test="endDate != null and endDate != ''">
                AND u.SYS_INSERT_DTM &lt;= #{endDate}::timestamp
            </if>
        </where>
        ORDER BY u.USER_ID
    </select>

    <insert id="insert">
        INSERT INTO CHMM_USER_INFO (
            <include refid="UserInfoColumns"/>
        ) VALUES (
            #{userId}, #{userEmail}, #{userMobile}, #{userName}, #{userNick}, #{userPwd}, #{userMsg}, #{userDesc}, 
            #{userStatCd}, #{userSnsid}, #{useYn}, 
            NOW(), #{sysInsertUserId}, NOW(), #{sysUpdateUserId}
        )
    </insert>

    <update id="update">
        UPDATE CHMM_USER_INFO
        SET USER_EMAIL = #{userEmail},
            USER_MOBILE = #{userMobile},
            USER_NAME = #{userName},
            USER_NICK = #{userNick},
            USER_MSG = #{userMsg},
            USER_DESC = #{userDesc},
            USER_STAT_CD = #{userStatCd},
            USER_SNSID = #{userSnsid},
            USE_YN = #{useYn},
            SYS_UPDATE_DTM = NOW(),
            SYS_UPDATE_USER_ID = #{sysUpdateUserId}
        WHERE USER_ID = #{userId}
    </update>

    <delete id="delete">
        DELETE FROM CHMM_USER_INFO
        WHERE USER_ID = #{userId}
    </delete>
</mapper>
