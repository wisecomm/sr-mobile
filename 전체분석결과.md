# 📊 SR 모바일 프로젝트 분석 결과

> 분석일: 2026년 1월 27일

## 1. 프로젝트 개요

**SR 모바일 프로젝트** - Next.js + Spring Boot 기반 풀스택 관리자 대시보드 웹 애플리케이션

---

## 2. 기술 스택

| 영역             | 기술                                                           |
| ---------------- | -------------------------------------------------------------- |
| **프론트엔드**   | Next.js 16 (App Router), React 19, TypeScript 5, TailwindCSS 4 |
| **UI 컴포넌트**  | Shadcn/UI, Radix UI                                            |
| **상태관리**     | TanStack React Query 5, Zustand                                |
| **폼/검증**      | React Hook Form 7, Zod 4                                       |
| **백엔드**       | Spring Boot 3.4.1, Java 21, MyBatis                            |
| **데이터베이스** | PostgreSQL (Flyway 마이그레이션)                               |
| **인증**         | JWT (jjwt 0.12.6), Spring Security                             |
| **테스트**       | Playwright (E2E 123개), JUnit 5                                |
| **빌드도구**     | pnpm (FE), Gradle (BE)                                         |

---

## 3. 프론트엔드 아키텍처

### 3.1 라우트 구조 (`app/`)

```
app/
├── (admin)/              # 관리자 영역 (인증 필요)
│   ├── (with-header)/    # 헤더가 있는 페이지
│   │   ├── boards/       # 게시판 관리
│   │   ├── menus/        # 메뉴 관리
│   │   ├── orders/       # 주문 관리
│   │   ├── roles/        # 권한 관리
│   │   ├── users/        # 사용자 관리
│   │   └── so-grid/      # 그리드 컴포넌트
│   ├── (without-header)/ # 헤더 없는 페이지
│   └── (mobile)/         # 모바일 전용 페이지
├── (guest)/              # 비인증 영역
│   ├── login/            # 로그인
│   ├── welcome/          # 웰컴 페이지
│   └── about/            # 소개 페이지
└── (playground)/         # 개발용 플레이그라운드
```

### 3.2 Feature-based 아키텍처 패턴

```mermaid
graph TD
    Page[Page Component<br/>(page.tsx)]:::ui --> ManagementHook[Management Hook<br/>(hooks/use-*-management.ts)]:::logic
    ManagementHook --> QueryHook[Query Hook<br/>(hooks/use-*-query.ts)]:::data
    QueryHook --> API[API Client<br/>(lib/api-client)]:::core
    Page --> Components[UI Components<br/>(Dialogs, Tables)]:::ui
```

| 계층                  | 역할                                             |
| --------------------- | ------------------------------------------------ |
| `page.tsx`            | 뷰/컴포지션 (UI 렌더링만)                        |
| `use-*-management.ts` | Presenter/Controller (상태, CRUD 오케스트레이션) |
| `use-*-query.ts`      | Data Access Layer (React Query, API 호출)        |

### 3.3 주요 의존성

```json
{
  "next": "16.0.3",
  "react": "19.2.0",
  "@tanstack/react-query": "^5.90.16",
  "zustand": "^5.0.8",
  "react-hook-form": "^7.69.0",
  "zod": "^4.2.1",
  "tailwindcss": "^4",
  "lucide-react": "^0.554.0"
}
```

---

## 4. 백엔드 아키텍처

### 4.1 패키지 구조 (`com.example.springrest`)

```
├── domain/               # 도메인별 비즈니스 로직
│   ├── auth/             # 인증/인가
│   ├── boards/           # 게시판
│   ├── menu/             # 메뉴
│   ├── order/            # 주문
│   ├── role/             # 권한
│   └── user/             # 사용자
├── global/               # 전역 설정/유틸
│   ├── config/           # 설정 클래스
│   ├── exception/        # 예외 처리
│   ├── filter/           # 필터 (JWT 등)
│   ├── model/            # 공통 모델
│   ├── security/         # Spring Security
│   └── util/             # 유틸리티
└── common/               # 공통 컴포넌트
```

### 4.2 주요 의존성

| 라이브러리           | 용도               |
| -------------------- | ------------------ |
| Spring Boot 3.4.1    | 핵심 프레임워크    |
| MyBatis + PageHelper | ORM + 페이지네이션 |
| Swagger/OpenAPI      | API 문서화         |
| Jasypt               | 설정 암호화        |
| Apache POI           | Excel 처리         |
| Flyway               | DB 마이그레이션    |
| JJWT 0.12.6          | JWT 토큰 처리      |

---

## 5. 주요 기능 모듈

| 기능          | 프론트엔드 경로  | 설명                  |
| ------------- | ---------------- | --------------------- |
| 사용자 관리   | `/users`         | CRUD + 권한 할당      |
| 권한 관리     | `/roles`         | RBAC + 메뉴 권한 매핑 |
| 메뉴 관리     | `/menus`         | 트리 구조 동적 메뉴   |
| 게시판 마스터 | `/boards/master` | 게시판 타입 설정      |
| 게시물 관리   | `/boards/board`  | CRUD + 파일 업로드    |
| 주문 관리     | `/orders`        | 주문 처리             |

### 5.1 기능별 파일 구조 예시 (Users)

```
app/(admin)/(with-header)/users/
├── page.tsx                      # 메인 뷰
├── columns.tsx                   # 테이블 컬럼 정의
├── user-dialog.tsx               # 생성/수정 다이얼로그
└── hooks/
    ├── use-user-management.ts    # 비즈니스 로직
    └── use-user-query.ts         # API 호출
```

---

## 6. 테스트 현황

### 6.1 E2E 테스트 (Playwright)

| 구분          | 파일                    | 테스트 범위                |
| ------------- | ----------------------- | -------------------------- |
| 메뉴          | `menus.spec.ts`         | 메뉴 트리 CRUD             |
| 사용자        | `users.spec.ts`         | 사용자 생성/검색/삭제      |
| 권한          | `roles.spec.ts`         | 권한 생성/삭제 + 메뉴 매핑 |
| 게시판 마스터 | `boards-master.spec.ts` | 게시판 마스터 CRUD         |
| 게시물        | `boards-post.spec.ts`   | 게시물 CRUD + 파일 업로드  |
| 주문          | `orders.spec.ts`        | 주문 관리                  |
| 네비게이션    | `navigation.spec.ts`    | 네비게이션 테스트          |

**총 123개 E2E 테스트**

### 6.2 테스트 실행 명령어

```bash
cd nextjs-client

# 전체 테스트
npx playwright test

# 특정 테스트만
npx playwright test tests/users.spec.ts

# HTML 리포트
npx playwright show-report
```

---

## 7. 빌드 & 배포

### 7.1 개발 환경

```bash
# 프론트엔드
cd nextjs-client
pnpm install
pnpm dev       # http://localhost:3000

# 백엔드
cd spring-rest
./gradlew bootRun   # http://localhost:8080
```

### 7.2 프로덕션 빌드

```bash
# Spring Boot + Next.js 통합 빌드 (정적 빌드 자동 적용)
cd spring-rest
./gradlew build
```

> **참고**: `gradlew build` 실행 시 `STATIC_EXPORT=true` 환경변수가 자동 설정되어 Next.js가 정적 빌드됩니다.

### 7.3 배포용 빌드

```bash
cd nextjs-client

# 개발 환경 배포 빌드
pnpm build:deploy:dev

# 프로덕션 배포 빌드
pnpm build:deploy:prod
```

---

## 8. 프로젝트 특징

1. **모노레포 구조**: 프론트엔드/백엔드가 한 저장소에 공존
2. **Feature-based 아키텍처**: 기능별 관심사 분리
3. **자체 그리드 라이브러리**: `so-grid-core`, `so-grid-react` 사용
4. **JWT 기반 인증**: Access Token + Refresh Token
5. **정적 빌드 지원**: 조건부로 Next.js 정적 내보내기 가능
6. **타입 안전성**: Zod 스키마 검증 + TypeScript strict mode
7. **보안**: Jasypt를 통한 민감 설정 암호화

---

## 9. 디렉토리 구조 요약

```
sr-mobile/
├── nextjs-client/           # 프론트엔드
│   ├── app/                 # Next.js App Router
│   │   ├── (admin)/         # 관리자 페이지
│   │   ├── (guest)/         # 비인증 페이지
│   │   └── api/             # API 라우트
│   ├── components/          # UI 컴포넌트 (Shadcn)
│   ├── hooks/               # 공통 React 훅
│   ├── lib/                 # API 클라이언트, 유틸
│   ├── store/               # Zustand 스토어
│   ├── types/               # TypeScript 타입
│   └── tests/               # Playwright 테스트
├── spring-rest/             # 백엔드
│   ├── src/main/java/       # Java 소스
│   │   └── com/example/springrest/
│   │       ├── domain/      # 도메인 로직
│   │       ├── global/      # 전역 설정
│   │       └── common/      # 공통 컴포넌트
│   └── build.gradle         # Gradle 빌드
└── README.md                # 프로젝트 문서
```
